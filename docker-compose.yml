version: "3.8"
services:
  ekas-data:
    image: omollo/ekas-data-portal-prod
    container_name: ekas-data-portal
    deploy:
      replicas: 5
    ports:
      - "8083:8083"
      - "7001:7001"
    networks:
      - host
    volumes:
      - logs:/go/logs
    depends_on:
      - "redis"

  redis:
    image: "redis:alpine"
    hostname: redis
    container_name: redis
    ports:
      - "6379:6379"
volumes:
  logs:
networks:
  host: